import{createClient}from'https://esm.sh/@supabase/supabase-js@2';const sb=createClient('https://jbdjfmvdrwdfnuhqrprc.supabase.co','sb_publishable_3ABEFAwN_wzmSu13EyVOwQ_h5Xfmz80',{auth:{persistSession:true,autoRefreshToken:true}}),$=id=>document.getElementById(id),money=n=>'R$ '+Number(n||0).toFixed(2).replace('.',','),d0=()=>{let d=new Date();d.setHours(0,0,0,0);return d};function dt(v){if(!v)return'—';let p=String(v).slice(0,10).split('-');return p[2]+'/'+p[1]+'/'+p[0]}function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}function col(id,c){let e=$(id)?.closest('.card');if(e){e.classList.remove('gp-green','gp-red','gp-orange');e.classList.add(c)}}function addCss(){if($('gpScopeCss'))return;let s=document.createElement('style');s.id='gpScopeCss';s.textContent='.gp-green{border-color:#16a34a!important}.gp-green .num{color:#4ade80!important}.gp-red{border-color:#dc2626!important}.gp-red .num{color:#f87171!important}.gp-orange{border-color:#f59e0b!important}.gp-orange .num{color:#fbbf24!important}.gp-scope-note{margin:10px 0;padding:10px;border:1px solid #40365c;border-radius:8px;color:#aaa;font-size:12px}';document.head.appendChild(s)}async function run(){const{data:{user}}=await sb.auth.getUser();if(!user)return;const{data:p}=await sb.from('profiles').select('organization_id,role').eq('id',user.id).maybeSingle();if(!p?.organization_id)return;const manager=['master','owner','admin'].includes(p.role),filter=q=>manager?q.is('seller_user_id',null):q.eq('seller_user_id',user.id),first=new Date(new Date().getFullYear(),new Date().getMonth(),1);let c=filter(sb.from('clients').select('id,name,whatsapp,value,due_date,status,server_id,seller_user_id')).order('due_date'),s=filter(sb.from('servers').select('id,name,purchase_cost,monthly_cost,credit_balance,credit_cost,active,seller_user_id')).order('name'),e=filter(sb.from('expenses').select('amount,incurred_at,seller_user_id')).gte('incurred_at',first.toISOString()),t=filter(sb.from('credit_transactions').select('server_id,type,quantity,unit_cost,total_cost,created_at,seller_user_id')).gte('created_at',first.toISOString()).order('created_at',{ascending:false});const[a,b,x,y]=await Promise.all([c,s,e,t]);if(a.error||b.error)return;const C=a.data||[],S=b.data||[],E=x.data||[],T=y.data||[],now=d0(),week=new Date(now);week.setDate(week.getDate()+7),active=C.filter(c=>c.status==='active'),soon=C.filter(c=>c.due_date&&new Date(String(c.due_date).slice(0,10)+'T00:00:00')>=now&&new Date(String(c.due_date).slice(0,10)+'T00:00:00')<=week),expired=C.filter(c=>c.due_date&&new Date(String(c.due_date).slice(0,10)+'T00:00:00')<now),rev=active.reduce((a,c)=>a+ +c.value||0,0),monthly=S.reduce((a,s)=>a+ +s.monthly_cost||0,0),serverBuy=S.reduce((a,s)=>a+ +s.purchase_cost||0,0),exp=E.reduce((a,e)=>a+ +e.amount||0,0),creditSpend=T.filter(x=>x.type==='purchase').reduce((a,x)=>a+(+x.total_cost||((+x.quantity||0)*(+x.unit_cost||0))),0),creditQty=T.filter(x=>x.type==='purchase').reduce((a,x)=>a+Math.abs(+x.quantity||0),0),creditBalance=S.reduce((a,s)=>a+ +s.credit_balance||0,0),op=rev-monthly-exp,real=op-serverBuy-creditSpend;const vals={total:C.length,activeCount:active.length,soonCount:soon.length,expiredCount:expired.length,revenue:money(rev),serverCost:money(monthly),serverPurchase:money(serverBuy),expenseCost:money(exp),operatingProfit:money(op),profit:money(real),fRevenue:money(rev),fServer:money(monthly),fPurchase:money(serverBuy+creditSpend),fExpense:money(exp),fOperating:money(op),fProfit:money(real)};for(const[k,v]of Object.entries(vals))if($(k))$(k).textContent=v;col('activeCount','gp-green');col('soonCount','gp-orange');col('expiredCount','gp-red');col('revenue','gp-green');col('serverCost',monthly?'gp-red':'gp-green');col('serverPurchase',serverBuy+creditSpend?'gp-red':'gp-green');col('expenseCost','gp-red');col('operatingProfit',op>=0?'gp-green':'gp-red');col('profit',real>=0?'gp-green':'gp-red');col('fRevenue','gp-green');col('fServer',monthly?'gp-red':'gp-green');col('fPurchase',serverBuy+creditSpend?'gp-red':'gp-green');col('fExpense','gp-red');col('fOperating',op>=0?'gp-green':'gp-red');col('fProfit',real>=0?'gp-green':'gp-red');const sn=id=>S.find(s=>s.id===id)?.name||'—';if($('dashRows'))$('dashRows').innerHTML=soon.concat(expired).slice(0,12).map(c=>{let n=String(c.whatsapp||'').replace(/\D/g,'');if(n&&!n.startsWith('55'))n='55'+n;return'<tr><td>'+esc(c.name)+'</td><td>'+esc(sn(c.server_id))+'</td><td>'+dt(c.due_date)+'</td><td>'+money(c.value)+'</td><td>'+(n?'<a href="https://wa.me/'+n+'" target="_blank"><button class="wa">WhatsApp</button></a>':'—')+'</td></tr>'}).join('')||'<tr><td colspan="5">Nenhum próximo vencimento.</td></tr>';let note=$('gpScopeNote');if(!note){note=document.createElement('div');note.id='gpScopeNote';note.className='gp-scope-note';$('dash')?.prepend(note)}if(note)note.textContent=manager?'Seu painel mostra somente os dados do proprietário. Para ver um vendedor, use a área Vendedores.':'Seu painel mostra somente seus clientes, servidores, créditos e despesas.';let credit=$('gpCreditOwnerSummary');if(!credit){credit=document.createElement('div');credit.id='gpCreditOwnerSummary';credit.className='gp-scope-note';$('dash')?.appendChild(credit)}if(credit)credit.innerHTML='💳 <b>Créditos deste painel:</b> '+creditQty+' comprados · '+money(creditSpend)+' gastos no mês · '+creditBalance+' em saldo.'}addCss();run();setInterval(run,15000);new MutationObserver(()=>setTimeout(run,400)).observe(document.body,{childList:true,subtree:true});